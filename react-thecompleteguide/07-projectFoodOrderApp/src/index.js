/* This project uses hooks, context and providers to show an example of order app which you can add items to the cart
and then you can see the cart component in a modal. The items of the cart are passed by the provider */
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
