/* This component Wrapper retuns the childrens, the usage of this is for when we want to avoid the nested divs, because react only renders one element */
const Wrapper = props => {
    return props.childern;
};

export default Wrapper;