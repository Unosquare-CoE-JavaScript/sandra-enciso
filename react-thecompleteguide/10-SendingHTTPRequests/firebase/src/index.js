/* This project is similar to the SWAPI project, but in this project we use a dummy backend (FireBase) to store and provide data */
import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
