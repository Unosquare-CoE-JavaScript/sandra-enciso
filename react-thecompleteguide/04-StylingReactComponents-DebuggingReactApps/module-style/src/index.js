/* This project is the same project of register goals, but its different using the styles check the component CouseInput */
import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
